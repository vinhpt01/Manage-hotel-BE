export enum StatusStaff {
    ACTIVE = 'active',
    DEACTIVE = 'deactive',
}

export enum StatusAccount {
    PENDDING = 'pendding',
    APPROVE = 'approve',
    REJECT = 'reject',
}

export enum Role {
    ADMIN = 'admin',
    USER = 'user',
}
